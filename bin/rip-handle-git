#!/usr/bin/env ruby
# Usage: rip-handle-git source

source = ARGV[0]

if source =~ %r{^file://|^git://|\.git$} ||
    File.directory?(source) && File.exists?("#{source}/HEAD")
  exit 0
else
  exit 1
end
