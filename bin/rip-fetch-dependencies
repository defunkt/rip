#!/usr/bin/env ruby
# Usage: rip-fetch-dependencies deps.rip
# Recursively downloads dependencies.

require 'rip/script'

Rip::Environment.new(ARGV * ' ').packages.each do |package|
  puts rip("package #{package.source} #{package.version}")
end
